version: '1.0'
name: data-pilot-flow
displayName: flow
triggers:
  trigger: auto
  push:
    tags:
      prefix:
        - 2
    paths:
      include:
        - ^data-pilot-flow/.*$
variables:
  APPLICATION: data-pilot-flow
  REGISTRY_NAMESPACE: jinmu-public
  SOURCE_DIR: data-pilot-flow
stages:
  - name: scan
    displayName: 扫描
    strategy: naturally
    trigger: auto
    executor: [ ]
    steps:
      - step: sc@opensca
        name: open_sca
        displayName: OpenSCA 检测
        detectPath: ./${SOURCE_DIR}/
        notify: [ ]
        strategy:
          retry: '0'
  - name: build
    displayName: 构建
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@docker
        name: build_docker
        displayName: 构建镜像
        type: cert
        certificate: aliyun-cr-jinmu-public
        tag: ${REGISTRY_NAMESPACE}/${APPLICATION}:${GITEE_BRANCH}
        dockerfile: Dockerfile
        context: .
        artifacts: []
        isCache: false
        parameter:
          SOURCE_DIR: ${SOURCE_DIR}
        notify: []
        strategy:
          retry: '0'
          timeout: 20
      - step: build@docker
        name: docker_build
        displayName: 镜像构建
        type: cert
        certificate: aliyun-cr-jinmu-public
        tag: ${REGISTRY_NAMESPACE}/${APPLICATION}:latest
        dockerfile: Dockerfile
        context: .
        artifacts: []
        isCache: false
        parameter:
          SOURCE_DIR: ${SOURCE_DIR}
        notify: []
        strategy:
          retry: '0'
          timeout: 20
